{% extends "::layout/layout.html.twig" %}        

{% block body %}
    <div class="container">
        <div class="row">
            
            <div class="span3">
                {{ include('includeTexte/teste.html.twig') }}

            </div>

            <div class="span9">
                <h2 style='text-align: center'> Attestation</h2>
                {% if attestations == null %}
                    <p>
                        <H3 class = "text-info"> Pas d'attestations disponi !!!</H3>
                        {{ include('includeType/no_view.html.twig') }}
                        </ p>
                    {% endif %}
                    {% if attestations|default is not empty %}

                    <table class="table table-striped table-hover center-block">
                        <thead>
                            <tr>
                                <th style="text-align: center">N° Attestation</th>
                                <th style="text-align: center">Lieu</th>
                                <th style="text-align: center">Date</th>
                                <th style="text-align: center">Consulter</th>
                                <th style="text-align: center">Voir Attes.</th>
                            </tr>
                        </thead>
                        <tbody>

                            {% for attestation in attestations %}
                                <tr> 
                                  <td style="text-align: center"> {{ attestation.numeroAttestation }} </td>
                                  <td style="text-align: center"> {{ attestation.lieu }} </td> 
                                  <td style="text-align: center"> {{ attestation.date|date('d/m/Y') }} </td>
                                  <td style="text-align: center"> {{ attestation.date|date('d/m/Y') }} </td>
                                  <td style="text-align: center"> <a href="{{ path('police_attestation_view_agent', { 'id' : attestation.id }) }}" target="_bank"><i class="icon-eye-open"></i></a> </td>

                                </tr>
                            {%endfor%}
                        {%endif%}

                    </tbody>
                    </table>
                                    
                        <div class="row">
                            <div class="col-md-12 text-center">
                                
                                    <div class="pagination text-center">
                                        
                                        <ul>
                                            {% for p in range(1, nbrTotalPages) %}
                                            <li{% if p == page %} class="active"{% endif %}>
                                                <a href="{{ path('police_attestation_policier', {'page': p}) }}">{{ p }}</a>
                                            </li>
                                             {% endfor %}
                                        </ul>
                                    </div>
                               
                            </div>
                        </div>
                                        
                                             <a class="icon-arrow-left" style="text-decoration: none; color: blue;" href="{{ path('police_attestation') }}">
                                    Retour
                                </a>                   
            </div>


        </div>

    {% endblock %}
