{% extends "::layout/layout.html.twig" %}        

{% block body %}
    <div class="container">
        <div class="row">
            <h2 style='text-align: center'> Attestation</h2>
            <div class="span3">
                {{ include('includeTexte/teste.html.twig') }}

            </div>

            <div class="span9">
                {% if attestations == null %}
                    <p>
                        <H3 class = "text-info"> Pas d'attestations disponible !!!</H3>
                        </ p>
                    {% endif %}
                    {% if attestations|default is not empty %}

                    <table class="table table-striped table-hover center-block">
                        <thead>
                            <tr>
                                <th>N° Attestation</th>
                                <th>Lieu</th>
                                <th>Date</th>
                                <th>Consulter</th>
                            </tr>
                        </thead>
                        <tbody>

                            {% for attestation in attestations %}
                                <tr> 
                                    <td> {{ attestation.numeroAttestation }} </td>
                                    <td> {{ attestation.aLieu }} </td> 
                                    <td> {{ attestation.date|date('d/m/Y') }} </td>
                                    <td> {{ attestation.date|date('d/m/Y') }} </td>
                                    <td> <a href="{{ path('police_attestation_view_agent', { 'id' : attestation.id }) }}"><i class="icon-eye-open"></i></a> </td>

                                </tr>
                            {%endfor%}
                        {%endif%}

                    </tbody>
                    </table>
                                    
                        <div class="row">
                            <div class="col-md-12 text-center">
                                
                                    <div class="pagination text-center">
                                        
                                        <ul>
                                            {% for p in range(1, nbrTotalPages) %}
                                            <li{% if p == page %} class="active"{% endif %}>
                                                <a href="{{ path('police_attestation_policier', {'page': p}) }}">{{ p }}</a>
                                            </li>
                                             {% endfor %}
                                        </ul>
                                    </div>
                               
                            </div>
                        </div>
                                        
                                             <a class="icon-arrow-left" style="text-decoration: none; color: blue;" href="{{ path('police_attestation') }}">
                                    Retour
                                </a>                   
            </div>


        </div>

    {% endblock %}
